# ব্যবহারকারীর গাইড — Couple's Private Vault

এই ডকুমেন্টটা টেকনিক্যাল মানুষদের জন্য না — যে কেউ পড়েই অ্যাপটা সেটআপ ও ব্যবহার করতে পারবে, সেভাবে লেখা।

## এই অ্যাপটা আসলে কী

এটা একটা **সম্পূর্ণ প্রাইভেট, নিজের সার্ভারে হোস্ট করা** অ্যাপ — শুধুমাত্র **স্বামী-স্ত্রীর** জন্য। এখানে দুজন একসাথে টেক্সট, ছবি, ভিডিও, ভয়েস নোট শেয়ার করতে পারবেন, চ্যাট করতে পারবেন — সম্পূর্ণ প্রাইভেটভাবে। কোনো Facebook, Google বা অন্য কোনো কোম্পানির সার্ভারে এই ডেটা যায় না — শুধু আপনার নিজের সার্ভারে থাকে, আর সেটাও এমনভাবে এনক্রিপ্ট করা থাকে যে সার্ভারের মালিকও (মানে যিনি এটা হোস্ট করছেন) আসল কনটেন্ট দেখতে পারবেন না।

> ⚠️ **জরুরি কথা:** এটা Google Play Store বা App Store-এ নেই, আর কখনও থাকবেও না (ইচ্ছাকৃতভাবে)। তাই এটা "ডাউনলোড করে চাপ দিলেই চলবে" — এরকম কোনো অ্যাপ না। এটা ব্যবহার করতে হলে দুটো ধাপ লাগবে:

1. **একবার সেটআপ** — একজনকে (আপনি বা পরিচিত কেউ যিনি একটু টেকনিক্যাল) একটা সার্ভার (VPS) ভাড়া নিয়ে ব্যাকএন্ড চালু করতে হবে। এটা এক-দুইবার করলেই হয়ে যায়।
2. **রোজকার ব্যবহার** — এরপর দুজনেই ফোনে অ্যাপ ইনস্টল করে সহজে ব্যবহার করতে পারবেন, ফেসবুকের মতোই সহজ।

নিচে দুটো ধাপই আলাদা করে বোঝানো হলো।

---

## ধাপ ১: একবারের সেটআপ (সার্ভার চালু করা)

এই অংশটা একজনকেই করতে হবে (দুজনের প্রতিদিন করা লাগবে না)।

### যা যা লাগবে

- একটা **VPS (Virtual Private Server)** — DigitalOcean, Hetzner, Contabo, বা যেকোনো ক্লাউড প্রোভাইডার থেকে মাসিক কয়েক ডলারে পাওয়া যায়। Ubuntu 22.04 বা তার পরের ভার্সন বেছে নিন।
- (ঐচ্ছিক) একটা **ডোমেইন নেইম** — না থাকলেও চলবে, শুধু IP address দিয়ে টেস্ট করা যাবে।

### সেটআপ করার ধাপ

1. আপনার VPS-এ SSH দিয়ে ঢুকুন (টার্মিনাল থেকে, বা PuTTY-এর মতো টুল দিয়ে)।
2. এই কমান্ডগুলো চালান:

   ```bash
   git clone https://github.com/mamun-qamarix/parsonal.git couple-vault
   cd couple-vault
   ./install.sh
   ```

3. `install.sh` স্ক্রিপ্টটা নিজে থেকেই:
   - Docker ইনস্টল করে দেবে (যদি না থাকে)
   - আপনার কাছে শুধু ডোমেইন নেইম (বা IP) জিজ্ঞেস করবে
   - বাকি সব পাসওয়ার্ড/সিক্রেট নিজে থেকে র‍্যান্ডম জেনারেট করে নেবে
   - সব সার্ভিস (ডেটাবেস, মিডিয়া স্টোরেজ, ফেস-রিকগনিশন সার্ভিস, ব্যাকএন্ড, HTTPS) চালু করে দেবে

4. শেষে একটা লিংক দেখাবে, যেমন: `https://আপনার-ডোমেইন/admin` — এটা ব্রাউজারে খুলুন।

5. প্রথমবার একটা **অ্যাডমিন পাসওয়ার্ড** সেট করতে বলবে — এটা সেট করুন (এটা শুধু সেটআপের জন্য, রোজকার ব্যবহারে লাগবে না)।

6. **ফেস-ভেরিফিকেশন সার্ভিস** সাধারণত `install.sh` নিজে থেকেই সেটআপ করে ফেলে, আলাদা কিছু করা লাগে না। ইনস্টলের শেষে যদি "Automatic setup didn't complete" লেখা দেখায়, তাহলে একটা ছোট ম্যানুয়াল ধাপ লাগবে — বিস্তারিত পদ্ধতি [README.md](README.md)-এর "CompreFace setup" অংশে লেখা আছে। এটা না করলেও বাকি সব অ্যাপ চলবে, শুধু ফেস-ভেরিফিকেশন কাজ করবে না।

এই পর্যন্ত হয়ে গেলে সার্ভার প্রস্তুত। এখন দুজনের ফোনে অ্যাপ ইনস্টল করার পালা।

---

## ধাপ ২: ফোনে অ্যাপ ইনস্টল করা (দুজনের জন্যই)

যেহেতু এটা Play Store-এ নেই, তাই "APK" ফাইল হিসেবে ইনস্টল করতে হয়। দুটো উপায় আছে:

### সহজ উপায় — রেডি APK ডাউনলোড করা

সরাসরি এখান থেকে APK ফাইলটা দুই ফোনেই ডাউনলোড করুন, আলাদা করে বানানোর দরকার নেই:

**[⬇️ couple-vault-v1.1.2.apk ডাউনলোড করুন](https://github.com/mamun-qamarix/parsonal/releases/download/apk-v1.1.2/couple-vault-v1.1.2.apk)**

(ফোনের ব্রাউজার দিয়ে লিংকে গিয়ে সরাসরি ডাউনলোড করা যাবে, অথবা কম্পিউটারে ডাউনলোড করে WhatsApp/USB দিয়ে ফোনে পাঠানো যাবে।)

ফোনে ফাইলটাতে ট্যাপ করে ইনস্টল করুন। প্রথমবার Android একটা সতর্কবার্তা দেখাতে পারে ("Install from unknown source") — এটা স্বাভাবিক, যেহেতু Play Store থেকে না। "Install anyway" চাপুন।

> কোডে কোনো পরিবর্তন করলে (যেমন অ্যাপের নাম বদলানো), তখন নতুন করে APK বানাতে হবে — নিচের "ডেভেলপারদের জন্য" অংশ দেখুন।

### ডেভেলপারদের জন্য — নিজে APK বানানো

```bash
cd couple-vault/mobile_app
flutter build apk --release
```

এর ফলে একটা ফাইল তৈরি হবে: `build/app/outputs/flutter-apk/app-release.apk` — এটা দুই ফোনেই পাঠান আগের মতোই।

---

## ধাপ ৩: প্রথমবার অ্যাপ চালু করা (Onboarding)

### অ্যাডমিন প্যানেল থেকে কোড বানানো

1. যিনি সার্ভার সেটআপ করেছেন, তিনি `https://আপনার-ডোমেইন/admin` পেজে লগইন করে **"Generate Setup Code"** বাটনে চাপ দেবেন।
2. একটা QR কোড আর একটা টেক্সট কোড দেখাবে। এটা ২৪ ঘণ্টার জন্য বৈধ।

### প্রতিটা ফোনে আলাদাভাবে

স্বামী ও স্ত্রী — দুজনেই নিজের ফোনে, নিজের মতো করে এই ধাপগুলো করবেন (একই ডিভাইস থেকে করা লাগবে না):

1. অ্যাপ খুলুন → **"সেটআপ কোড স্ক্যান করুন"** চাপুন → উপরের QR কোডটা স্ক্যান করুন। (স্ক্যান করতে সমস্যা হলে "কোড হাতে টাইপ করে দিন" দিয়ে টেক্সট কোডটা বসিয়ে দিন।)
2. **"স্বামী" নাকি "স্ত্রী"** বেছে নিন — যে যার নিজেরটা বেছে নেবে। (একবার একজন "স্বামী" বেছে নিলে, অন্যজন আর সেটা বেছে নিতে পারবেন না — শুধু "স্ত্রী" বেছে নিতে হবে, এবং উল্টোটাও।)
3. একটা **পাসওয়ার্ড** সেট করুন (কমপক্ষে ৮ অক্ষর)।
4. **অথেন্টিকেটর অ্যাপ যুক্ত করুন** — এই ধাপে একটা QR কোড দেখাবে। আপনার ফোনে **Google Authenticator** বা **Authy** অ্যাপ (Play Store থেকে ইনস্টল করুন, এগুলো Google/Twilio-র সাধারণ ফ্রি অ্যাপ) খুলে সেই QR কোডটা স্ক্যান করুন — এতে অ্যাপে একটা নতুন এন্ট্রি তৈরি হবে যেটা প্রতি ৩০ সেকেন্ডে একটা নতুন ৬-সংখ্যার কোড দেখাবে। সেই কোডটা Couple's Vault অ্যাপে টাইপ করে "নিশ্চিত করুন" চাপুন।

ব্যস, সেটআপ শেষ! এখন থেকে অ্যাপ খুলতে **পাসওয়ার্ড + অথেন্টিকেটর অ্যাপের কোড** — দুটোই লাগবে, প্রতিবার।

> চাইলে পরে **Settings → মুখ ভেরিফিকেশন (ঐচ্ছিক)** থেকে মুখ দিয়ে বাড়তি একটা যাচাইও চালু করে রাখতে পারেন — এটা পুরোপুরি ঐচ্ছিক।

---

## ধাপ ৪: রোজকার ব্যবহার

- **হোম স্ক্রিন** — উপরে "Husband" / "Wife" ট্যাব, তার নিচে Text / Photo / Video ট্যাব। নিচে ডান কোণে **+** বাটনে চেপে নতুন কিছু যোগ করুন।
- **যেকোনো কিছু এডিট বা ডিলিট করতে চাইলে** — সেটা সাথে সাথে হয় না, সঙ্গীর কাছে অনুরোধ যায়, তিনি "হ্যাঁ" বললেই পরিবর্তন হয়। এটা ইচ্ছাকৃতভাবেই রাখা হয়েছে, যাতে একজনের ভুলে কিছু হারিয়ে না যায়।
- **Reel** — নিচের নেভিগেশনে ছবি/ভিডিওগুলো TikTok-এর মতো স্ক্রল করে দেখা যায়। দুজনেই ❤️ দিলে একটা "ম্যাচ" এনিমেশন দেখাবে।
- **Chat** — রিয়েল-টাইম চ্যাট, ছবি/ভিডিও/ভয়েস/ফাইল পাঠানো যায়।
- **Profile** — নিজের নাম, ছবি, বিশেষ দিনগুলো (anniversary), আর **Wishlist** (কী চান তার লিস্ট) রাখা যায়।
- **Favorite Lines** — স্বামী-স্ত্রী একে অপরকে যেসব বিশেষ কথা/লাইন লিখতে চান, সেগুলোর জন্য আলাদা জায়গা।
- **History / Favorites** — হোম স্ক্রিনের উপরের আইকন থেকে সব পুরোনো কার্যকলাপ বা ❤️ দেওয়া জিনিস দেখা যায়।
- **Countdown** — পরের বার একসাথে দেখা হওয়ার তারিখ বসিয়ে রাখলে হোম স্ক্রিনে গোনা দেখাবে।

### নিরাপত্তা সংক্রান্ত ফিচার (Settings থেকে)

- **Auto-lock** — কিছুক্ষণ অ্যাপ ব্যবহার না করলে এমনিই লক হয়ে যাবে।
- **মুখ ভেরিফিকেশন (ঐচ্ছিক)** — অন করলে লগইনে পাসওয়ার্ড + অথেন্টিকেটর কোডের পাশাপাশি মুখ দিয়েও যাচাই করতে হবে, বাড়তি নিরাপত্তার জন্য। ডিফল্টে বন্ধ থাকে।
- **Duress PIN (বিপদের পিন)** — আসল পাসওয়ার্ডের বদলে এই পিনটা দিলে একটা ভুয়া/খালি স্ক্রিন দেখাবে, যাতে কেউ জোর করে ফোন খুলতে বললেও আসল কনটেন্ট দেখতে না পায়।
- **হোম স্ক্রিনের নাম বদলানো** — অ্যাপটার আইকন/নাম "Notes" বা "Calculator" বানিয়ে রাখা যায়, যাতে ফোনের হোম স্ক্রিনে সহজে চোখে না পড়ে।
- স্ক্রিনশট/স্ক্রিন-রেকর্ডিং এমনিতেই বন্ধ থাকে অ্যাপের ভেতরে।

---

## সচরাচর জিজ্ঞাসিত প্রশ্ন (FAQ)

**প্রশ্ন: এই ডেটা কি Google/Facebook বা অন্য কোথাও যায়?**
না। শুধু আপনার নিজের ভাড়া করা সার্ভারে থাকে, আর সেটাও এনক্রিপ্ট করা অবস্থায়। বিস্তারিত টেকনিক্যাল ব্যাখ্যা চাইলে [DECISIONS.md](DECISIONS.md)-এর ১নং পয়েন্ট দেখুন।

**প্রশ্ন: সার্ভারের মালিক (যে হোস্ট করেছে) কি আমাদের ছবি/মেসেজ দেখতে পারবে?**
না। এনক্রিপশন এমনভাবে করা যে সার্ভার শুধু "এলোমেলো ডেটা" দেখে, আসল কনটেন্ট শুধু দুটো ফোনেই ডিক্রিপ্ট হয়।

**প্রশ্ন: আমরা দুজন কি একই ফোন থেকে সেটআপ করতে হবে?**
না, দুজন আলাদা ফোন থেকে আলাদাভাবে সেটআপ করতে পারবেন।

**প্রশ্ন: পাসওয়ার্ড ভুলে গেলে?**
দুজনকেই নিজের নিজের অথেন্টিকেটর অ্যাপের কোড দিয়ে ভেরিফাই করতে হবে, তারপর নতুন পাসওয়ার্ড সেট করা যাবে (একা একজনের পক্ষে রিসেট করা যায় না, ইচ্ছাকৃতভাবেই)।

**প্রশ্ন: অথেন্টিকেটর অ্যাপ থেকে ফোন হারিয়ে/বদলে গেলে?**
নতুন ফোনে Google Authenticator/Authy ইনস্টল করে সেটআপের সময় দেখানো "কী" (secret key) হাতে টাইপ করে আবার যোগ করতে পারবেন — তাই সেটআপের সময় "কী হাতে টাইপ করুন" অপশন থেকে key-টা কোথাও নিরাপদে টুকে রাখা ভালো অভ্যাস।

**প্রশ্ন: iPhone-এ চলবে?**
এই মুহূর্তে অ্যান্ড্রয়েডে সম্পূর্ণ টেস্ট করা হয়েছে। iOS-এর জন্য কোড আছে কিন্তু এখনো বিল্ড-টেস্ট করা হয়নি।

**প্রশ্ন: টেকনিক্যাল সমস্যা হলে কী করব?**
প্রথমে [README.md](README.md) দেখুন — সেখানে ডেভেলপার/টেকনিক্যাল ডিটেইল আছে। GitHub রিপোতে একটা "Issue" খুলে সমস্যাটা লিখতে পারেন।

---

## গুরুত্বপূর্ণ সীমাবদ্ধতা

- এটা ঠিক দুইজন ব্যবহারকারীর জন্য বানানো (এক স্বামী + এক স্ত্রী)। তৃতীয় কেউ যোগ হতে পারবে না।
- সার্ভার বন্ধ হয়ে গেলে বা মুছে গেলে ডেটা পুনরুদ্ধারের কোনো "ব্যাকআপ" সিস্টেম ইচ্ছাকৃতভাবে রাখা হয়নি (নিরাপত্তার কারণে — বিস্তারিত [DECISIONS.md](DECISIONS.md)-এ)। তাই সার্ভার/VPS-এর নিজস্ব ব্যাকআপ ব্যবস্থা রাখা আপনার দায়িত্ব।
- এটা টেকনিক্যাল প্রজেক্ট — সার্ভার সেটআপে সমস্যা হলে একজন ডেভেলপারের সাহায্য লাগতে পারে।
