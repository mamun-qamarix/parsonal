# ব্যবহারকারীর গাইড — Couple's Private Vault

এই ডকুমেন্টটা টেকনিক্যাল মানুষদের জন্য না — যে কেউ পড়েই অ্যাপটা সেটআপ ও ব্যবহার করতে পারবে, সেভাবে লেখা।

## এই অ্যাপটা আসলে কী

এটা একটা **সম্পূর্ণ প্রাইভেট, নিজের সার্ভারে হোস্ট করা** অ্যাপ — শুধুমাত্র **স্বামী-স্ত্রীর** জন্য। এখানে দুজন একসাথে টেক্সট, ছবি, ভিডিও, ভয়েস নোট শেয়ার করতে পারবেন, চ্যাট করতে পারবেন — সম্পূর্ণ প্রাইভেটভাবে। কোনো Facebook, Google বা অন্য কোনো কোম্পানির সার্ভারে এই ডেটা যায় না — শুধু আপনার নিজের সার্ভারে থাকে, আর সেটাও এমনভাবে এনক্রিপ্ট করা থাকে যে সার্ভারের মালিকও (মানে যিনি এটা হোস্ট করছেন) আসল কনটেন্ট দেখতে পারবেন না।

> ⚠️ **জরুরি কথা:** এটা Google Play Store বা App Store-এ নেই, আর কখনও থাকবেও না (ইচ্ছাকৃতভাবে)। তাই এটা "ডাউনলোড করে চাপ দিলেই চলবে" — এরকম কোনো অ্যাপ না। এটা ব্যবহার করতে হলে দুটো ধাপ লাগবে:

1. **একবার সেটআপ** — একজনকে (আপনি বা পরিচিত কেউ যিনি একটু টেকনিক্যাল) একটা সার্ভার (VPS) ভাড়া নিয়ে ব্যাকএন্ড চালু করতে হবে। এটা এক-দুইবার করলেই হয়ে যায়।
2. **রোজকার ব্যবহার** — এরপর দুজনেই ফোনে অ্যাপ ইনস্টল করে সহজে ব্যবহার করতে পারবেন, ফেসবুকের মতোই সহজ।

নিচে দুটো ধাপই আলাদা করে বোঝানো হলো।

---

## ধাপ ১: একবারের সেটআপ (সার্ভার চালু করা)

এই অংশটা একজনকেই করতে হবে (দুজনের প্রতিদিন করা লাগবে না)।

### যা যা লাগবে

- একটা **VPS (Virtual Private Server)** — DigitalOcean, Hetzner, Contabo, বা যেকোনো ক্লাউড প্রোভাইডার থেকে মাসিক কয়েক ডলারে পাওয়া যায়। Ubuntu 22.04 বা তার পরের ভার্সন বেছে নিন।
- (ঐচ্ছিক) একটা **ডোমেইন নেইম** — না থাকলেও চলবে, শুধু IP address দিয়ে টেস্ট করা যাবে।

### সেটআপ করার ধাপ

1. আপনার VPS-এ SSH দিয়ে ঢুকুন (টার্মিনাল থেকে, বা PuTTY-এর মতো টুল দিয়ে)।
2. এই কমান্ডগুলো চালান:

   ```bash
   git clone https://github.com/mamun-qamarix/parsonal.git couple-vault
   cd couple-vault
   ./install.sh
   ```

3. `install.sh` স্ক্রিপ্টটা নিজে থেকেই:
   - Docker ইনস্টল করে দেবে (যদি না থাকে)
   - আপনার কাছে শুধু ডোমেইন নেইম (বা IP) জিজ্ঞেস করবে
   - বাকি সব পাসওয়ার্ড/সিক্রেট নিজে থেকে র‍্যান্ডম জেনারেট করে নেবে
   - সব সার্ভিস (ডেটাবেস, মিডিয়া স্টোরেজ, ফেস-রিকগনিশন সার্ভিস, ব্যাকএন্ড, HTTPS) চালু করে দেবে

4. শেষে একটা লিংক দেখাবে, যেমন: `https://আপনার-ডোমেইন/admin` — এটা ব্রাউজারে খুলুন।

5. প্রথমবার একটা **অ্যাডমিন পাসওয়ার্ড** সেট করতে বলবে — এটা সেট করুন (এটা শুধু সেটআপের জন্য, রোজকার ব্যবহারে লাগবে না)।

6. **ফেস-ভেরিফিকেশন সার্ভিস** সাধারণত `install.sh` নিজে থেকেই সেটআপ করে ফেলে, আলাদা কিছু করা লাগে না। ইনস্টলের শেষে যদি "Automatic setup didn't complete" লেখা দেখায়, তাহলে একটা ছোট ম্যানুয়াল ধাপ লাগবে — বিস্তারিত পদ্ধতি [README.md](README.md)-এর "CompreFace setup" অংশে লেখা আছে। এটা না করলেও বাকি সব অ্যাপ চলবে, শুধু ফেস-ভেরিফিকেশন কাজ করবে না।

7. **পুশ নোটিফিকেশন (ঐচ্ছিক, তবে জোরালোভাবে সুপারিশ করা হয়)** — অ্যাপ খোলা থাকলে নোটিফিকেশন এমনিতেই আসে। কিন্তু অ্যাপ বন্ধ/ব্যাকগ্রাউন্ডে থাকা অবস্থাতেও নোটিফিকেশন পেতে চাইলে:
   - Firebase Console-এ একটা প্রজেক্ট বানিয়ে সার্ভিস-অ্যাকাউন্ট কী (JSON ফাইল) ডাউনলোড করুন এবং VPS-এ `backend/firebase-service-account.json` নামে সেভ করুন (এটা একটা সিক্রেট — কখনও GitHub-এ যাবে না, `.gitignore`-এ আগে থেকেই বাদ দেওয়া আছে)।
   - VPS-এর `.env` ফাইলে যোগ করুন: `FCM_SERVICE_ACCOUNT_PATH=/srv/firebase-service-account.json`
   - চালান: `docker compose up -d --build backend`
   - বিস্তারিত ধাপ [README.md](README.md)-এর "Push notifications" অংশে আছে।

   এটা না করলেও অ্যাপ পুরোপুরি কাজ করবে — শুধু অ্যাপ বন্ধ থাকা অবস্থায় নোটিফিকেশন আসবে না। নোটিফিকেশনের লেখা সবসময় জেনেরিক থাকে (যেমন "নতুন একটা মেসেজ এসেছে", "নতুন একটা ছবি এসেছে") — ভেতরের আসল লেখা/ছবি/ভিডিওর কোনো তথ্য কখনও নোটিফিকেশনে দেখানো হয় না।

এই পর্যন্ত হয়ে গেলে সার্ভার প্রস্তুত। এখন দুজনের ফোনে অ্যাপ ইনস্টল করার পালা।

---

## ধাপ ২: ফোনে অ্যাপ ইনস্টল করা (দুজনের জন্যই)

যেহেতু এটা Play Store-এ নেই, তাই "APK" ফাইল হিসেবে ইনস্টল করতে হয়। দুটো উপায় আছে:

### সহজ উপায় — রেডি APK ডাউনলোড করা

সরাসরি এখান থেকে APK ফাইলটা দুই ফোনেই ডাউনলোড করুন, আলাদা করে বানানোর দরকার নেই:

**[⬇️ couple-vault-v2.9.2.apk ডাউনলোড করুন](https://github.com/mamun-qamarix/parsonal/releases/download/apk-v2.9.2/couple-vault-v2.9.2.apk)**

*(নতুন এই ভার্সনে: পুশ নোটিফিকেশনে এখন অ্যাপের নিজস্ব একটা আলাদা সাউন্ড বাজবে (যেটা
মেসেজ পাওয়ার সময় অ্যাপের ভিতরেও বাজে) -- শুনেই বোঝা যাবে এটা এই অ্যাপ থেকে এসেছে।
চ্যাটে এখন রিয়েল-টাইমে "সঙ্গী টাইপ করছেন..." এবং "সঙ্গী ভয়েস রেকর্ড করছেন..." দেখা
যাবে।)*

*(নতুন এই ভার্সনে: চ্যাট, ক্যাপশন, কমেন্ট, উইশলিস্ট, প্রিয় লাইন -- যেখানেই কোনো লিংক
(http/www দিয়ে শুরু) লেখা থাকবে, সেটা এখন নীল রঙে দেখাবে এবং ট্যাপ করলে সরাসরি ব্রাউজারে
খুলে যাবে।)*

*(APK-এর সাইজ এখনো ~৪০MB। এই ভার্সনে: অ্যাপ এখন শুধু ডার্ক থিমে চলে (লাইট থিম বাদ,
আর কোনো হালকা/গাঢ় সবুজের আলাদা হিসাব নেই -- একটাই রং সব জায়গায়), চ্যাটের প্রাইভেসি
বাটন এখন হোমের প্রাইভেসি বাটন থেকে সম্পূর্ণ স্বাধীন, বটম নেভিগেশনে ব্যাক বাটনে চাপলে
আগে হোম ট্যাবে আসবে (সরাসরি অ্যাপ থেকে বের হয়ে যাবে না), ব্যাকগ্রাউন্ড থেকে ফিরে
আসলে আগের ট্যাবেই থাকবেন, চ্যাট খোলার সাথে সাথে সবশেষ মেসেজেই থাকবেন (মাঝখানে না),
চ্যাটে উপরে স্ক্রল করলে নিচে যাওয়ার একটা ফ্লোটিং বাটন আসবে, চ্যাটের মেসেজ ধরে রাখলে
(long press) ইমোজি দিয়ে রিয়্যাক্ট করা যাবে, এবং মেসেজ ডান দিকে টান দিলে (swipe) সেই
নির্দিষ্ট মেসেজের রিপ্লাই পাঠানো যাবে। এছাড়া আগের ভার্সনগুলোর সব ফিচার: প্রাইভেসি
মাস্ক পুরো অ্যাপ জুড়ে, হোম ফিড এলোমেলো ক্রম, "সব" ফিল্টার, মুখ ভেরিফিকেশন বন্ধ,
এক-লাইনের কাউন্টডাউন, পাশাপাশি অ্যাকশন আইকন, সম্পূর্ণ-স্ক্রিন পোস্ট লেআউট, প্রিয়
লাইনে ফিঙ্গারপ্রিন্ট, "গোপন মুহূর্তের মোড", এবং সম্পূর্ণ বাংলা UI।)*

(ফোনের ব্রাউজার দিয়ে লিংকে গিয়ে সরাসরি ডাউনলোড করা যাবে, অথবা কম্পিউটারে ডাউনলোড করে WhatsApp/USB দিয়ে ফোনে পাঠানো যাবে।)

ফোনে ফাইলটাতে ট্যাপ করে ইনস্টল করুন। প্রথমবার Android একটা সতর্কবার্তা দেখাতে পারে ("Install from unknown source") — এটা স্বাভাবিক, যেহেতু Play Store থেকে না। "Install anyway" চাপুন।

> কোডে কোনো পরিবর্তন করলে (যেমন অ্যাপের নাম বদলানো), তখন নতুন করে APK বানাতে হবে — নিচের "ডেভেলপারদের জন্য" অংশ দেখুন।

### ডেভেলপারদের জন্য — নিজে APK বানানো

```bash
cd couple-vault/mobile_app
flutter build apk --release --split-per-abi
```

এর ফলে কয়েকটা ফাইল তৈরি হবে (প্রতিটা ফোনের প্রসেসর টাইপ অনুযায়ী আলাদা) --
`build/app/outputs/flutter-apk/app-arm64-v8a-release.apk` (~৪০MB) ফাইলটাই বেশিরভাগ
আধুনিক ফোনের জন্য ঠিক আছে -- এটাই দুই ফোনে পাঠান। (আগে একটাই "ইউনিভার্সাল" APK
বানানো হতো যেটার সাইজ ১০০MB-র বেশি ছিল -- সব প্রসেসরের জন্য ডুপ্লিকেট কোড থাকার
কারণে।)

---

## ধাপ ৩: প্রথমবার অ্যাপ চালু করা (Onboarding)

### অ্যাডমিন প্যানেল থেকে কোড বানানো

1. যিনি সার্ভার সেটআপ করেছেন, তিনি `https://আপনার-ডোমেইন/admin` পেজে লগইন করে **"Generate Setup Code"** বাটনে চাপ দেবেন।
2. একটা QR কোড আর একটা টেক্সট কোড দেখাবে। এটা ২৪ ঘণ্টার জন্য বৈধ।

### প্রতিটা ফোনে আলাদাভাবে

স্বামী ও স্ত্রী — দুজনেই নিজের ফোনে, নিজের মতো করে এই ধাপগুলো করবেন (একই ডিভাইস থেকে করা লাগবে না):

1. অ্যাপ খুলুন → **"সেটআপ কোড স্ক্যান করুন"** চাপুন → উপরের QR কোডটা স্ক্যান করুন। (স্ক্যান করতে সমস্যা হলে কোডের টেক্সটটা উপরের ছোট বক্সে পেস্ট করে দিন।)
2. **"স্বামী" নাকি "স্ত্রী"** বেছে নিন — যে যার নিজেরটা বেছে নেবে। (একবার একজন "স্বামী" বেছে নিলে, অন্যজন আর সেটা বেছে নিতে পারবেন না — শুধু "স্ত্রী" বেছে নিতে হবে, এবং উল্টোটাও।)
3. একটা **পাসওয়ার্ড** সেট করুন (কমপক্ষে ৮ অক্ষর)।

> ⚠️ **মনে রাখবেন:** "স্বামী" বা "স্ত্রী" রোলটা সারা জীবনে ঠিক **একবারই** এভাবে বেছে নেওয়া (claim করা) যায় — টেস্ট করার জন্যও না। তাই "স্বামী" রোলটা শুধু স্বামীর আসল ফোনেই বেছে নিন, "স্ত্রী" রোলটা শুধু স্ত্রীর আসল ফোনেই। ভুল করে একই ফোন থেকে দুটো রোলই বেছে নিলে, বা পরে ফোন হারালে/বদলালে — চিন্তা নেই, নিচের "নতুন ডিভাইস যোগ করুন" অংশে সমাধান আছে।

ব্যস, সেটআপ শেষ! লগইন হয়েই যাবে। এরপর থেকে **প্রতি ঘণ্টায় একবার পাসওয়ার্ড** দিতে হবে, আর মাঝের সময়টায় অ্যাপ খুললেই শুধু **ফিঙ্গারপ্রিন্ট** দিলে চলবে (নিচে "নিরাপত্তা সংক্রান্ত ফিচার" অংশে বিস্তারিত)।

> চাইলে পরে **Settings → মুখ ভেরিফিকেশন (ঐচ্ছিক)** থেকে মুখ দিয়ে বাড়তি একটা যাচাইও চালু করে রাখতে পারেন — এটা পুরোপুরি ঐচ্ছিক।

---

## ধাপ ৪: রোজকার ব্যবহার

- **হোম স্ক্রিন** — টেক্সট/ছবি/ভিডিও সব মিশিয়ে একটাই ফিড (সময় অনুযায়ী), উপরে ফিল্টার বাটন দিয়ে টেক্সট/ছবি/ভিডিও ও স্বামী/স্ত্রী — একসাথে যেকোনো কম্বিনেশনে ফিল্টার করা যায়। নিচে ডান কোণে **+** বাটনে চেপে নতুন কিছু যোগ করুন (কোন ধরনের — টেক্সট/ছবি/ভিডিও — জিজ্ঞেস করবে)।
- **যেকোনো কিছু এডিট বা ডিলিট করতে চাইলে** — সেটা সাথে সাথে হয় না, সঙ্গীর কাছে অনুরোধ যায়, তিনি "হ্যাঁ" বললেই পরিবর্তন হয়। এটা ইচ্ছাকৃতভাবেই রাখা হয়েছে, যাতে একজনের ভুলে কিছু হারিয়ে না যায়।
- **Reel** — নিচের নেভিগেশনে ছবি/ভিডিওগুলো TikTok-এর মতো স্ক্রল করে দেখা যায়। দুজনেই ❤️ দিলে একটা "ম্যাচ" এনিমেশন দেখাবে।
- **Chat** — রিয়েল-টাইম চ্যাট, ছবি/ভিডিও/ভয়েস/ফাইল পাঠানো যায়। পাঠানো মেসেজে সিন-টিক দেখাবে (✓ পাঠানো, ✓✓ ডেলিভার্ড, নীল ✓✓ দেখা হয়েছে)। মেসেজ পাঠালে/এলে আলাদা শব্দ হবে। ভয়েস মেসেজ রেকর্ড করার সময় WhatsApp-এর মতো লাইভ ওয়েভফর্ম-টাইমার দেখাবে (থামানো/আবার শুরু করা যায়), আর পাঠানো ভয়েস সরাসরি প্লে করা যায়। ছবি/ভিডিওতে চাপলে বড় করে (জুম করেও) দেখা যায়। উপরে চোখ-আইকনে চাপলে সব মেসেজ শুধু নিজের ফোনে তারকা (★) দিয়ে লুকিয়ে যাবে (সঙ্গীর ফোনে প্রভাব পড়বে না)।
- **Profile** — নিজের নাম, ছবি, বিশেষ দিনগুলো (anniversary), আর **Wishlist** (কী চান তার লিস্ট) রাখা যায়।
- **আমাদের প্রিয় লাইন** — স্বামী-স্ত্রী একে অপরকে যেসব বিশেষ কথা/লাইন লিখতে চান, সেগুলোর জন্য আলাদা জায়গা (আলাদা "স্বামী → স্ত্রী" ও "স্ত্রী → স্বামী" ট্যাব, একে অপরের লেখা রেটিং দেওয়া যায়) — **Profile ট্যাবে** স্ক্রল করলে একটা কার্ড হিসেবে দেখা যাবে।
- **History / Favorites** — হোম স্ক্রিনের উপরের আইকন থেকে সব পুরোনো কার্যকলাপ বা ❤️ দেওয়া জিনিস দেখা যায়।
- **Countdown** — পরের বার একসাথে দেখা হওয়ার তারিখ বসিয়ে রাখলে হোম স্ক্রিনে গোনা দেখাবে।

### নিরাপত্তা সংক্রান্ত ফিচার (Settings থেকে)

- **পাসওয়ার্ড + ফিঙ্গারপ্রিন্ট** — শেষবার পাসওয়ার্ড দেওয়ার পর ১ ঘণ্টা পর্যন্ত অ্যাপ খুললে শুধু ফিঙ্গারপ্রিন্ট (বা ফোনের ফেস-আনলক/পিন) দিলেই ঢোকা যাবে, বারবার অ্যাপ বন্ধ-খোলা করলেও পাসওয়ার্ড লাগবে না। ১ ঘণ্টা পার হয়ে গেলে আবার পাসওয়ার্ড দিতে হবে। ফিঙ্গারপ্রিন্ট কাজ না করলে "পাসওয়ার্ড দিয়ে ঢুকুন" বাটন থেকে সরাসরি পাসওয়ার্ড দেওয়া যায়।
- **Auto-lock** — কিছুক্ষণ অ্যাপ ব্যবহার না করলে এমনিই লক হয়ে যাবে (উপরের নিয়ম অনুযায়ী পাসওয়ার্ড বা ফিঙ্গারপ্রিন্ট চাইবে)।
- **মুখ ভেরিফিকেশন (ঐচ্ছিক)** — অন করলে লগইনে পাসওয়ার্ডের পাশাপাশি মুখ দিয়েও যাচাই করতে হবে, বাড়তি নিরাপত্তার জন্য। ডিফল্টে বন্ধ থাকে।
- **Duress PIN (বিপদের পিন)** — আসল পাসওয়ার্ডের বদলে এই পিনটা দিলে একটা ভুয়া/খালি স্ক্রিন দেখাবে, যাতে কেউ জোর করে ফোন খুলতে বললেও আসল কনটেন্ট দেখতে না পায়।
- **হোম স্ক্রিনের নাম বদলানো** — অ্যাপটার আইকন/নাম "Notes" বা "Calculator" বানিয়ে রাখা যায় (প্রতিটার নিজস্ব আলাদা আইকনও আছে), যাতে ফোনের হোম স্ক্রিনে সহজে চোখে না পড়ে।
- **পাসওয়ার্ড রিসেট অনুমোদন করুন** — অন্য কোনো ডিভাইসে (নিজের অন্য ফোন বা সঙ্গীর ফোন) কেউ পাসওয়ার্ড রিসেট শুরু করলে, এই ইতিমধ্যে-লগইন-করা ফোন থেকে সেই কোডটা দিয়ে অনুমোদন করা যায়।
- **ডিভাইসসমূহ** — আপনাদের দুজনের সব ডিভাইস (কোনটা স্বামীর, কোনটা স্ত্রীর) একসাথে দেখা যায়। ফোন হারালে বা বদলালে এখান থেকে সেই ডিভাইসটা সরিয়ে দিলে সেটা দিয়ে আর অ্যাপে ঢোকা যাবে না — আবার ঢুকতে হলে নতুন করে সেটআপ কোড দিয়ে যুক্ত করতে হবে। (v1.4.0-এর আগে বারবার লগইন করলে একই ফোনের জন্য একাধিক এন্ট্রি তৈরি হতো — এটা এখন ঠিক হয়ে গেছে; পুরোনো ডুপ্লিকেট এন্ট্রি থাকলে "এই ডিভাইস" ব্যাজ ছাড়া বাকিগুলো নিশ্চিন্তে মুছে ফেলা যাবে।)
- **নতুন ডিভাইস যোগ করুন** — আপনার রোল (স্বামী/স্ত্রী) ইতিমধ্যে একবার সেটআপ করা হয়ে গেছে, কিন্তু এখন সেই একই অ্যাকাউন্টটা আরেকটা ফোনেও (নতুন ফোন, বা ফোন বদলানোর কারণে) ব্যবহার করতে চান? নতুন করে সেটআপ কোড দিয়ে রোল আবার বেছে নেওয়া যাবে না (একবারই করা যায়) — তার বদলে:
  1. যে ফোনে অ্যাপ চালু আছে (লগইন করা আছে), সেখানে **Settings → নতুন ডিভাইস যোগ করুন** চাপুন — একটা QR কোড দেখাবে। (এই QR কোডে আপনার ভল্টের আসল চাবি আছে, তাই এটা শুধু নিজের আরেকটা ফোনেই স্ক্যান করান, অন্য কাউকে দেখাবেন না।)
  2. নতুন ফোনে অ্যাপ ইনস্টল করে খুলুন → স্বাগতম স্ক্রিনের নিচে **"ইতিমধ্যে অ্যাকাউন্ট আছে? এই ডিভাইসটা যোগ করুন"** চাপুন → উপরের QR কোডটা স্ক্যান করুন।
  3. নতুন ফোনে **একই পাসওয়ার্ড** দিন (পাসওয়ার্ড দুই ফোনেই একই থাকে) — ব্যস, একই অ্যাকাউন্ট এই ফোনেও চলে এলো।
- স্ক্রিনশট/স্ক্রিন-রেকর্ডিং এমনিতেই বন্ধ থাকে অ্যাপের ভেতরে।

---

## সচরাচর জিজ্ঞাসিত প্রশ্ন (FAQ)

**প্রশ্ন: এই ডেটা কি Google/Facebook বা অন্য কোথাও যায়?**
না। শুধু আপনার নিজের ভাড়া করা সার্ভারে থাকে, আর সেটাও এনক্রিপ্ট করা অবস্থায়। বিস্তারিত টেকনিক্যাল ব্যাখ্যা চাইলে [DECISIONS.md](DECISIONS.md)-এর ১নং পয়েন্ট দেখুন।

**প্রশ্ন: সার্ভারের মালিক (যে হোস্ট করেছে) কি আমাদের ছবি/মেসেজ দেখতে পারবে?**
না। এনক্রিপশন এমনভাবে করা যে সার্ভার শুধু "এলোমেলো ডেটা" দেখে, আসল কনটেন্ট শুধু দুটো ফোনেই ডিক্রিপ্ট হয়।

**প্রশ্ন: আমরা দুজন কি একই ফোন থেকে সেটআপ করতে হবে?**
না, দুজন আলাদা ফোন থেকে আলাদাভাবে সেটআপ করতে পারবেন।

**প্রশ্ন: পাসওয়ার্ড ভুলে গেলে?**
লক হয়ে থাকা ফোনে "পাসওয়ার্ড ভুলে গেছেন?" চাপলে একটা কোড দেখাবে। সেই কোডটা ইতিমধ্যে লগইন করা আছে এমন যেকোনো ফোনে (নিজের অন্য ফোন বা সঙ্গীর ফোন) নিয়ে গিয়ে **Settings → পাসওয়ার্ড রিসেট অনুমোদন করুন**-এ বসিয়ে অনুমোদন দিন। লক হয়ে থাকা ফোনে ফিরে এলেই নতুন পাসওয়ার্ড সেট করা যাবে (একা একজনের পক্ষে নিজে নিজে রিসেট করা যায় না, ইচ্ছাকৃতভাবেই)।

**প্রশ্ন: "husband already registered on this deployment" বা এই ধরনের এরর দেখাচ্ছে, কী করব?**
এর মানে ওই রোলটা (husband/wife) আগেই একবার claim করা হয়ে গেছে — হয়তো ভুল করে টেস্ট করার সময়, বা অন্য কোনো ফোনে। নতুন সেটআপ কোড দিয়ে সেই রোলটা আর দ্বিতীয়বার claim করা যাবে না। সমাধান: যে ফোনে ওই রোলটা দিয়ে আগে থেকেই লগইন করা আছে, সেখান থেকে **Settings → নতুন ডিভাইস যোগ করুন** ব্যবহার করে নতুন ফোনটা যোগ করুন (উপরের "ধাপ ৪"-এ বিস্তারিত আছে)। যদি কোনো ফোনেই লগইন করা না থাকে (দুটো রোলই হারিয়ে গেছে), তাহলে সার্ভারের ডেটাবেস রিসেট করতে হবে (`docker compose down -v && docker compose up -d --build`) — এতে আগের সব ডেটা মুছে যাবে, তাই এটা শেষ উপায়।

**প্রশ্ন: ফিঙ্গারপ্রিন্ট কাজ না করলে?**
BiometricUnlockScreen-এ "আবার চেষ্টা করুন" দিয়ে আবার চেষ্টা করুন, অথবা "পাসওয়ার্ড দিয়ে ঢুকুন" চেপে সরাসরি পাসওয়ার্ড দিয়ে ঢুকুন। ফোনে কোনো ফিঙ্গারপ্রিন্ট/ফেস-আনলক সেট করা না থাকলে ফোনের নিজস্ব পিন/প্যাটার্ন দিয়েও যাচাই করা যাবে (এটা ফোনের নিজস্ব সিস্টেম, অ্যাপের না)।

**প্রশ্ন: iPhone-এ চলবে?**
এই মুহূর্তে অ্যান্ড্রয়েডে সম্পূর্ণ টেস্ট করা হয়েছে। iOS-এর জন্য কোড আছে কিন্তু এখনো বিল্ড-টেস্ট করা হয়নি।

**প্রশ্ন: টেকনিক্যাল সমস্যা হলে কী করব?**
প্রথমে [README.md](README.md) দেখুন — সেখানে ডেভেলপার/টেকনিক্যাল ডিটেইল আছে। GitHub রিপোতে একটা "Issue" খুলে সমস্যাটা লিখতে পারেন।

---

## গুরুত্বপূর্ণ সীমাবদ্ধতা

- এটা ঠিক দুইজন ব্যবহারকারীর জন্য বানানো (এক স্বামী + এক স্ত্রী)। তৃতীয় কেউ যোগ হতে পারবে না।
- সার্ভার বন্ধ হয়ে গেলে বা মুছে গেলে ডেটা পুনরুদ্ধারের কোনো "ব্যাকআপ" সিস্টেম ইচ্ছাকৃতভাবে রাখা হয়নি (নিরাপত্তার কারণে — বিস্তারিত [DECISIONS.md](DECISIONS.md)-এ)। তাই সার্ভার/VPS-এর নিজস্ব ব্যাকআপ ব্যবস্থা রাখা আপনার দায়িত্ব।
- এটা টেকনিক্যাল প্রজেক্ট — সার্ভার সেটআপে সমস্যা হলে একজন ডেভেলপারের সাহায্য লাগতে পারে।
