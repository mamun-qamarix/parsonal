org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# Windows-only: project (D:) and Gradle/pub caches (C:) are on different
# drives, which crashes Kotlin's incremental-compilation relocatable file
# cache (IllegalArgumentException: different roots). Disabling incremental
# Kotlin compilation avoids it; does not affect correctness, only rebuild speed.
kotlin.incremental=false
